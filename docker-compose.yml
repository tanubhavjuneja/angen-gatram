version: '3.8'

services:
  tsk:
    image: cincan/sleuthkit:latest
    container_name: forensic_tsk
    volumes:
      - ./evidence:/evidence
      - ./output:/output
    working_dir: /evidence
    stdin_open: true
    tty: true
